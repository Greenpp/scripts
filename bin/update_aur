#!/bin/sh

for DIR in $HOME/AUR/*; do
    cd $DIR

    echo -n "Checking $(basename $DIR) ... "

    result=$(git pull)
    if [ "$result" = "Already up to date." ]; then
        echo "Nothing to do"
    else
        echo "New update"
    fi

    cd ..
done

