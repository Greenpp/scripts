#!/bin/bash

if [[ -f $HOME/.unicode_symbols ]]; then
    chosen=$(cat $HOME/.unicode_symbols | dmenu -i -l 20 -fn 'monospace-18' )

    [[ $chosen != "" ]] || exit

    echo $chosen | cut -d ' ' -f1 | xclip -selection clipboard
fi
